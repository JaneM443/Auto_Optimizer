python3 -m venv env
source env/bin/activate
gflops: 9

with open("hpl.log","r") as f:

    summary_line = f.readlines()[38]
    print(summary_line)

    # Split the line using whitespace as delimiter and extract the Gflops value
    columns = summary_line.split()
    print(columns)
    gflops = float(columns[-1])

    print("Extracted Gflops:", gflops)


HPL Linpack benchmark input file
Innovative Computing Laboratory, University of Tennessee
HPL.out      output file name (if any)
6            device out (6=stdout,7=stderr,file)
1            # of problems sizes (N)
100000       Ns
1            # of NBs
128          NBs
0            PMAP process mapping (0=Row-,1=Column-major)
1            # of process grids (P x Q)
8            Ps
16           Qs
16.0         threshold
1            # of panel fact
2            PFACTs (0=left, 1=Crout, 2=Right)
1            # of recursive stopping criterium
4            NBMINs (>= 1)
1            # of panels in recursion
2            NDIVs
1            # of recursive panel fact.
1            RFACTs (0=left, 1=Crout, 2=Right)
1            # of broadcast
0            BCASTs (0=1rg,1=1rM,2=2rg,3=2rM,4=Lng,5=LnM)
1            # of lookahead depth
1            DEPTHs (>=0)
2            SWAP (0=bin-exch,1=long,2=mix)
64           swapping threshold
0            L1 in (0=transposed,1=no-transposed) form
0            U  in (0=transposed,1=no-transposed) form
1            Equilibration (0=no,1=yes)
8            memory alignment in double (> 0)

"""
Sampling Methods:

<1000 trials
    Are parameters Correlated?  --> yes --> Gaussian Processes
                                --> no  --> TPE

>1000 Trials
    CMA-ES
    

DEVICE = torch.device("cpu")
BATCHSIZE = 128
CLASSES = 10
DIR = os.getcwd()
EPOCHS = 10
N_TRAIN_EXAMPLES = BATCHSIZE * 30
N_VALID_EXAMPLES = BATCHSIZE * 10

near linear scclability

"""

#import os
#os.system ("run as in terminal") xx  - run command prompts within python
